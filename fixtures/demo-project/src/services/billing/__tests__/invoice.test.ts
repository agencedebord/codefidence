import { InvoiceService } from '../invoice';

describe('InvoiceService', () => {
  it('should create an invoice with validated data', async () => {
    const service = new InvoiceService({}, {});
    // test implementation
  });

  it('should calculate total from multiple invoices', async () => {
    const service = new InvoiceService({}, {});
    // test implementation
  });
});
